
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/syslog-ng/syslog-client/ataque.log
    scan_frequency: 1s
    ignore_older: 0
    close_inactive: 1m
    clean_inactive: 2m

output.logstash:
  hosts: ["logstash:5044"]
